const DatabaseManager = require('../database/DatabaseManager')

class Activity{
    //need to pass in email so we can query for the right User
    constructor(type, name, distance, blah, blah){
        //fill in fields with passed in stuff now
        return 
    }

    //functions to update activity

    //delete activity

}